package com.raiderrobotix._2020.commands.auton

import com.raiderrobotix._2020.subsystems.DriveBase

suspend fun defensiveAuton() {
    // Auton Test
    // Start on Left Player Station
    // add cowl
    // fireNBalls(3)
    // driveWithoutCorrection(displacement = -1.5*12, speed = 0.2)
    // delay(500)
    // turn(angularDisplacement = -10.0, speed = 0.2 )
    // delay(500)
    // driveWithoutCorrection(displacement = -12.0*12, speed = 0.5) // input correct distance

    DriveBase.speed = 0.0
}
